<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viandas Solidarias | Donaciones</title>
    <link rel="shortcut icon" href="./favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="../css/estilos.css">
    <style>
        .header {
            height: 14vh;
            padding: 10px 20px;
            position: sticky;
            top: 0;
            z-index: 10;
            align-items: center;
            display: flex;
            justify-content: space-between;
        }

        .formulario input,
        .formulario button,
        .formulario textarea,
        .formulario input[type="file"],
        .formulario select {
            display: block;
            width: calc(100% - 40px);
            margin: 0 auto 20px auto;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .mensaje-exito {
            color: green;
            text-align: center;
            font-weight: bold;
            font-size: 1.2em;
            margin-top: 20px;
        }

    </style>
</head>
<body>
<header class="header">
    <nav>
        <a href="/inicio">
            <img src="../assets/img/logo.png" alt="Logo de Viandas Solidarias" loading="lazy" width="100">
        </a>
        <ul>
            <li>
                <a href="/cerrarSesion">
                    <img src="../assets/img/cerrarSesion.png" alt="Cerrar sesión" loading="lazy">
                </a>
            </li>
        </ul>
    </nav>
</header>
<<main id="main" class="main">

            <form method="POST" action="/visitaTecnica" enctype="multipart/form-data" id="form-registro" class="formulario">
                <div class="nueva-colaboracion">
                    <h2>Registrar visita técnica</h2>

                    {{#if solicitudExitosa}}
                        <div class="mensaje-exito">{{visitaRegistrada}}</div>
                    {{/if}}

                    <p>Ingresa el día que realizó la visita técnica</p>
                    <div>
                        <input type="date" name="diaVisita" id="diaVisita">
                    </div>


                    <p>Seleccione una heladera</p>
                    <select name="heladera" id="heladera"  required>
                        {{#each heladeras}}
                            <option value="{{this.id}}">{{this.nombreHeladera}}</option>
                        {{/each}}
                    </select>

                    <p>Ingrese si pudo arreglar la heladera</p>
                    <div class="arreglado" >
                        <div>
                            <select name="arreglado" class="arreglado" id="arreglado" required>
                                <option value="SI">SI</option>
                                <option value="NO">NO</option>
                            </select>
                        </div>
                    </div>

                    <p>Ingrese una foto de su visita</p>
                    <input type="file" name="imagen" id="imagen">
                    <textarea name="descripcion" id="descripcion" placeholder="Descripción del trabajo" rows="4"></textarea>
                    <button type="submit">Enviar</button>
                </div>
            </form>
</main>

    <!-- FOOTER -->
    <footer class="footer">
        <nav>
            <ul>
                <li><a href="/termino">Términos y condiciones</a></li>
                <li><a href="/preguntasFrecuentes">Preguntas frecuentes</a></li>
                <li><a href="/contacto">Contacto</a></li>
            </ul>
        </nav>
        <a href="#" class="scroll-to-top" aria-label="Subir al inicio">
            <img src="../assets/img/flecha-hacia-arriba-blanca.png" alt="Ir arriba flecha">
        </a>
    </footer>
</body>
</html>